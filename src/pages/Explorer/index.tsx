import React from 'react';
// import { useApi } from '../../hooks';
import NodeInfo from '../../components/NodeInfo';

export default () => {
  // const { api } = useApi();

  return (
    <div>
      <h1>Page explorer</h1>
      <NodeInfo />
    </div>
  );
};
